name: Sync newsletter articles

on:
  workflow_dispatch:
  schedule:
    # 09:00 America/Sao_Paulo (UTC-3)
    - cron: "0 12 * * *"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: sync-newsletter-articles
  cancel-in-progress: false

jobs:
  sync:
    name: Render latest newsletter articles
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      AUTOMATION_BRANCH: automation/sync-newsletter-articles

    steps:
      - name: Checkout repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false

      - name: Render four latest articles
        run: python3 .github/scripts/render-newsletter.py

      - name: Generate newsletter RSS feed
        run: python3 .github/scripts/generate-newsletter-feed.py

      - name: Synchronize SEO metadata
        run: python3 .github/scripts/sync-seo.py

      - name: Validate generated content
        id: validation
        shell: bash
        run: |
          article_count="$(python3 - <<'PY'
          from pathlib import Path

          html = Path("index.html").read_text(encoding="utf-8")
          start = html.index("<!-- NEWSLETTER_ARTICLES:START -->")
          end = html.index("<!-- NEWSLETTER_ARTICLES:END -->", start)
          block = html[start:end]

          count = block.count("<article>")
          if count < 1 or count > 4:
              raise SystemExit(f"Quantidade inesperada de artigos renderizados: {count}")

          if block.count('target="_blank"') != count:
              raise SystemExit("Nem todos os artigos possuem link externo esperado.")

          print(count)
          PY
          )"

          feed_count="$(python3 - <<'PY'
          import xml.etree.ElementTree as ET

          root = ET.parse("newsletter/feed.xml").getroot()
          if root.tag != "rss" or root.attrib.get("version") != "2.0":
              raise SystemExit("RSS 2.0 inválido.")

          items = root.findall("./channel/item")
          if not items:
              raise SystemExit("RSS sem artigos.")

          for item in items:
              for field in ("title", "link", "guid", "pubDate"):
                  if not (item.findtext(field) or "").strip():
                      raise SystemExit(f"Item RSS sem campo obrigatório: {field}")

          print(len(items))
          PY
          )"

          echo "article_count=${article_count}" >> "$GITHUB_OUTPUT"
          echo "feed_count=${feed_count}" >> "$GITHUB_OUTPUT"
          echo "Validated ${article_count} homepage article(s) and ${feed_count} RSS item(s)."
          git diff --check

      - name: Detect generated content changes
        id: changes
        shell: bash
        run: |
          if git diff --quiet -- index.html en/index.html newsletter/feed.xml sitemap.xml; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Generated newsletter and SEO content already up to date."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Generated newsletter or SEO content update detected."
          fi

      - name: Publish automation branch and open or update PR
        id: pull_request
        if: steps.changes.outputs.changed == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.AUTOMATION_PR_TOKEN || github.token }}
          GH_REPO: ${{ github.repository }}
          AUTOMATION_TOKEN_CONFIGURED: ${{ secrets.AUTOMATION_PR_TOKEN != '' }}
        run: |
          gh auth setup-git

          remote_sha="$(
            git ls-remote --heads origin "refs/heads/${AUTOMATION_BRANCH}" \
              | awk '{print $1}'
          )"

          git switch -c "$AUTOMATION_BRANCH"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html en/index.html newsletter/feed.xml sitemap.xml
          git commit -m "content: sync latest newsletter articles"

          if [[ -n "$remote_sha" ]]; then
            git push \
              --force-with-lease="refs/heads/${AUTOMATION_BRANCH}:${remote_sha}" \
              origin "HEAD:refs/heads/${AUTOMATION_BRANCH}"
          else
            git push --set-upstream origin "HEAD:refs/heads/${AUTOMATION_BRANCH}"
          fi

          cat > /tmp/newsletter-sync-pr.md <<EOF
          ## Resumo

          Atualiza automaticamente a homepage, o RSS público e os metadados SEO com os artigos mais recentes do catálogo da newsletter.

          ## Validação

          - bloco da newsletter renderizado por \`.github/scripts/render-newsletter.py\`;
          - RSS regenerado por \`.github/scripts/generate-newsletter-feed.py\`;
          - JSON-LD, RSS discovery e sitemap sincronizados por \`.github/scripts/sync-seo.py\`;
          - quantidade de artigos da homepage validada antes do commit;
          - XML e campos obrigatórios dos itens RSS validados antes do commit;
          - \`git diff --check\` executado;
          - quality gates do repositório devem passar antes do merge.

          ## Origem

          Gerado por [Sync newsletter articles](https://github.com/${GH_REPO}/actions/runs/${GITHUB_RUN_ID}).

          > Este PR é mantido pela automação. Novas sincronizações atualizam a mesma branch enquanto o PR estiver aberto.
          EOF

          pr_number="$(
            gh pr list \
              --repo "$GH_REPO" \
              --head "$AUTOMATION_BRANCH" \
              --base main \
              --state open \
              --json number \
              --jq '.[0].number // empty'
          )"

          if [[ -n "$pr_number" ]]; then
            gh pr edit "$pr_number" \
              --repo "$GH_REPO" \
              --title "content: sync latest newsletter articles" \
              --body-file /tmp/newsletter-sync-pr.md
            pr_url="$(gh pr view "$pr_number" --repo "$GH_REPO" --json url --jq '.url')"
          else
            pr_url="$(
              gh pr create \
                --repo "$GH_REPO" \
                --base main \
                --head "$AUTOMATION_BRANCH" \
                --title "content: sync latest newsletter articles" \
                --body-file /tmp/newsletter-sync-pr.md
            )"
            pr_number="$(gh pr view "$pr_url" --repo "$GH_REPO" --json number --jq '.number')"
          fi

          {
            echo "number=${pr_number}"
            echo "url=${pr_url}"
            echo "automation_token_configured=${AUTOMATION_TOKEN_CONFIGURED}"
          } >> "$GITHUB_OUTPUT"

      - name: Write synchronization summary
        if: always()
        shell: bash
        env:
          ARTICLE_COUNT: ${{ steps.validation.outputs.article_count }}
          FEED_COUNT: ${{ steps.validation.outputs.feed_count }}
          CONTENT_CHANGED: ${{ steps.changes.outputs.changed }}
          PR_NUMBER: ${{ steps.pull_request.outputs.number }}
          PR_URL: ${{ steps.pull_request.outputs.url }}
          AUTOMATION_TOKEN_CONFIGURED: ${{ steps.pull_request.outputs.automation_token_configured }}
        run: |
          {
            echo "# Newsletter synchronization"
            echo
            echo "- **Artigos renderizados na homepage:** ${ARTICLE_COUNT:-n/a}"
            echo "- **Itens publicados no RSS:** ${FEED_COUNT:-n/a}"
            echo "- **Conteúdo gerado alterado:** ${CONTENT_CHANGED:-n/a}"

            if [[ -n "$PR_URL" ]]; then
              echo "- **Branch:** \`${AUTOMATION_BRANCH}\`"
              echo "- **Pull Request:** [#${PR_NUMBER}](${PR_URL})"

              if [[ "$AUTOMATION_TOKEN_CONFIGURED" == "true" ]]; then
                echo "- **Token de automação dedicado:** configurado"
              else
                echo "- **Token de automação dedicado:** não configurado; o GitHub pode exigir aprovação manual para iniciar os workflows do PR criado pelo \`GITHUB_TOKEN\`."
              fi
            else
              echo "- **Pull Request:** não necessário"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
